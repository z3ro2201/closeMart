server:
    port: 8000

spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/myhometown?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
        username:
        password: 

#mybatis ?? ??
mybatis:
    # resources/mapper/ ? ?? ?? xml ??
    mapper-locations: mapper/*.xml